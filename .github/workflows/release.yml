name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # 四端构建并发布 GitHub Release（DMG ×2 / NSIS / deb+AppImage / APK）
  build-and-publish:
    uses: ./.github/workflows/build.yml
    with:
      release: true
